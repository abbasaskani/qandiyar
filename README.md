# قندیار (GhandYar) — v4
این نسخه مشکل `Failed to fetch` را حذف کرده: دیتای نمونه داخل `sample_data.js` است.

## نصب روی GitHub Pages
Settings → Pages → Deploy from a branch → main → /(root)

## نکته
Demo Mode فقط نمایشی است و عدد دوز واقعی ارائه نمی‌کند.
